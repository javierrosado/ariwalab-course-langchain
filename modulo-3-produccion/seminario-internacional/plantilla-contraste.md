# Documento de contraste — [equipo] · [track]

> Plantilla. Dos páginas, formativo, no ponderado. Se entrega en las 2 h asíncronas
> posteriores al Seminario.

---

## 1 · Los 3 casos, en una frase cada uno

| Panelista | Qué construyó, para qué |
|---|---|
| 1 | `<1 frase>` |
| 2 | `<1 frase>` |
| 3 | `<1 frase>` |

---

## 2 · Tres decisiones del panel que coinciden con las suyas

Para cada una: qué decisión, y por qué creen que coinciden.

1. `<decisión>` — `<por qué coinciden>`
2. `<decisión>` — `<por qué coinciden>`
3. `<decisión>` — `<por qué coinciden>`

---

## 3 · Dos decisiones que difieren

Para cada una: la decisión del panel, la suya, y **cuál de las dos defenderían ante un cliente**.

1. `<decisión del panel>` vs. `<la suya>` — defienden: `<cuál y por qué>`
2. `<decisión del panel>` vs. `<la suya>` — defienden: `<cuál y por qué>`

---

## 4 · Un fallo de producción que su agente también tendría

`<el fallo que oyeron, y por qué su propio diseño es vulnerable al mismo problema>`

Esta sección es la que más rinde. Un equipo que sale del curso sabiendo cómo va a fallar su
propio sistema aprendió más que uno que sale creyendo que no fallará.
